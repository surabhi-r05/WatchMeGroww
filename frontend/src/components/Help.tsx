import {BookOpen,X} from 'lucide-react';
import {useState} from 'react';
export default function Help(){const [open,setOpen]=useState(false);return <><button onClick={()=>setOpen(true)} className="help-btn" title="How to use Watch Me Groww"><BookOpen size={17}/> Help</button>{open&&<div className="modal-backdrop" onClick={()=>setOpen(false)}><div className="modal panel" onClick={e=>e.stopPropagation()}><div className="flex justify-between items-center"><div><div className="eyebrow">Quick guide</div><h2 className="text-2xl font-bold">How to use Watch Me Groww</h2></div><button className="icon-btn" onClick={()=>setOpen(false)}><X/></button></div><div className="space-y-4 mt-6 text-sm"><div><b>1. Start with your watchlist.</b><p className="muted mt-1">Open Dashboard or Watchlists to see what changed since your last review.</p></div><div><b>2. Open a stock.</b><p className="muted mt-1">Use search to inspect price action, technicals, signals, news and events.</p></div><div><b>3. Trust the labels.</b><p className="muted mt-1">Live, delayed, cached, demo and unavailable are shown explicitly. We never present demo data as live.</p></div><div><b>4. Set alerts.</b><p className="muted mt-1">Alerts run server-side and create notifications when their conditions are met.</p></div><div><b>5. Compare and investigate.</b><p className="muted mt-1">Use Markets, Compare and Insights to understand relative performance rather than relying on one number.</p></div></div></div></div>}</>}
